[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -m "not e2e"
markers =
    e2e: Tests de integración de entorno. Requieren servicios activos (API+Frontend). Ejecutar con: pytest -m e2e
